const calculateBtn = document.getElementById('calc-btn');
const resetBtn = document.getElementById('reset-btn');
const heightInput = document.getElementById('height-input');
const weigthInput = document.getElementById('weight-input');

const resetInputs = () => {
    heightInput.value = '';
    weigthInput.value = '';

}

const calculateBmi = () => {
  const enteredHeight = +heightInput.value;
  const enteredweight = +weigthInput.value;

  const bmi =  enteredweight / (enteredHeight * enteredHeight);

  console.log(bmi);
};



calculateBtn.addEventListener('click',calculateBmi);
resetBtn.addEventListener('click',resetInputs);const calculateBtn = document.getElementById('calc-btn');
const resetBtn = document.getElementById('reset-btn');
const heightInput = document.getElementById('height-input');
const weigthInput = document.getElementById('weight-input');

const resetInputs = () => {
    heightInput.value = '';
    weigthInput.value = '';

}

const calculateBmi = () => {
  const enteredHeight = +heightInput.value;
  const enteredweight = +weigthInput.value;

  const bmi =  enteredweight / (enteredHeight * enteredHeight);

  console.log(bmi);
};



calculateBtn.addEventListener('click',calculateBmi);
resetBtn.addEventListener('click',resetInputs);const calculateBtn = document.getElementById('calc-btn');
const resetBtn = document.getElementById('reset-btn');
const heightInput = document.getElementById('height-input');
const weigthInput = document.getElementById('weight-input');

const resetInputs = () => {
    heightInput.value = '';
    weigthInput.value = '';

}

const calculateBmi = () => {
  const enteredHeight = +heightInput.value;
  const enteredweight = +weigthInput.value;

  const bmi =  enteredweight / (enteredHeight * enteredHeight);

  console.log(bmi);
};



calculateBtn.addEventListener('click',calculateBmi);
resetBtn.addEventListener('click',resetInputs);const calculateBtn = document.getElementById('calc-btn');
const resetBtn = document.getElementById('reset-btn');
const heightInput = document.getElementById('height-input');
const weigthInput = document.getElementById('weight-input');

const resetInputs = () => {
    heightInput.value = '';
    weigthInput.value = '';

}

const calculateBmi = () => {
  const enteredHeight = +heightInput.value;
  const enteredweight = +weigthInput.value;

  const bmi =  enteredweight / (enteredHeight * enteredHeight);

  console.log(bmi);
};



calculateBtn.addEventListener('click',calculateBmi);
resetBtn.addEventListener('click',resetInputs);